# FILE PATH: requirements-dev.txt
# Dev/CI-only dependencies. Not required to run the ERP itself — see requirements.txt for that.
# [Session CS1] NEW FILE — GitHub Actions "ERP CI" / validate-and-test job installed only
# requirements.txt (fastapi, uvicorn, sqlalchemy, psycopg), which has no pytest in it.
# Confirmed live this session by reproducing the failure locally: a clean venv with only
# requirements.txt installed raised "No module named pytest" when running pytest. The
# migration-smoke job in the same workflow already installs pytest explicitly, which is why
# only validate-and-test would fail. tests/ also import fastapi.testclient's TestClient,
# which needs httpx as an extra dependency of fastapi's test client in recent fastapi/starlette
# releases, so it is pinned here too. This file does not change requirements.txt or any
# runtime app dependency.
pytest>=8.0,<9
httpx>=0.27,<1
