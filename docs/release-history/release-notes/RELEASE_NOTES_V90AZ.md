# V90.az — Batch Traceability + Recall / Withdrawal

Adds configurable forward traceability from packed FG lots to production batches, dispatches/customers/invoices and returns, plus auditable recall cases and controlled withdrawal actions.

Migration: 125
