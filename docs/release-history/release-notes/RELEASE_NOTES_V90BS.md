# V90.bs — Dispatch UI + Transporter + POD + Backorder/Reconciliation UI

Adds the operational dispatch Web UI on top of V90.ax: dispatch queue, KPI summary, transporter visibility, POD status, and open backorders/reconciliation visibility, with scoped RBAC and persisted screen preferences.

Migration: 144
