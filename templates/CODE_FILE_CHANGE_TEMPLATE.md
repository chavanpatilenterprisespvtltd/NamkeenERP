# Namkeen ERP — Complete Code File Change Template

Use this as the authoring template. Replace the placeholders and use the comment syntax appropriate to the target language.

```text
<COMMENT> FILE PATH: <full/repository/path>
<COMMENT> ─── <Component name> v<major>.<minor> (Session <N> — <one-line summary>) ─

<COMMENT> [Session N] FIX/FEATURE — <ALL-CAPS HEADLINE>.
<COMMENT> Confirmed live this session via <exact method> — <specific evidence>.
<COMMENT>
<COMMENT> ROOT CAUSE (if a fix): <underlying mechanism>.
<COMMENT>
<COMMENT> THE FIX: <exact function/mechanism and scope>. <Explicitly unaffected areas>.
<COMMENT> <Follow-up verification step, if applicable>.

<COMMENT> ─── v<older> HEADER (preserved) ───────────────────────────────────
<COMMENT> [Older Session] ...full older entry unchanged...

<implementation>

<COMMENT> [Session N] FIX — see file header. <short pointer to changed logic>.
<changed code>
```

**Delivery:** Replace the complete destination file. Do not send only the changed lines.
